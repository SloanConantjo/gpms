extends admin

block content
  h1= title
  p Welcome to #{title}
  
  table 
    tr
      th UserName 
      th UserLevel
    each acc in data 
      tr 
        th #{acc.userName}
        th #{acc.accLevel}